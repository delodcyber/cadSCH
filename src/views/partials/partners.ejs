<section class="section-block partners-section">
  <div class="container">
    <p class="section-kicker">IN PARTNERSHIP WITH</p>

    <div class="partner-row">
      <% partners.forEach(function(partner) { %>
        <div class="partner-chip"><%= partner %></div>
      <% }); %>
    </div>
  </div>
</section>
